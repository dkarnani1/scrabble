'use client';

export function UrgencyBadge() {
  return (
    <span
      data-testid="timer-urgent"
      className="inline-flex h-5 items-center rounded-full bg-premium-tw/15 px-2 text-[10px] font-semibold uppercase tracking-wide text-premium-tw"
    >
      hurry
    </span>
  );
}
