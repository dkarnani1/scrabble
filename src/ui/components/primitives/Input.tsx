'use client';

import * as React from 'react';
import { Input as ShadcnInput } from '@ui/components/ui/input';

export type InputProps = React.ComponentProps<typeof ShadcnInput>;

export const Input = ShadcnInput;
